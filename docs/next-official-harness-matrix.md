# Next official adapter harness matrix

This file records the current pass/defer/exclude state for the Next official deploy adapter suite.

**2026-06-23 — `test/e2e/middleware-rewrites` closed (webpack).** 9 failing assertions → 0 (57 pass / 1 skip). Fixes: i18n dynamic page/data route locale-prefixing and auto-static dynamic `_next/data` route emission (adapter routing-compiler/pages-static-data/artifact-planner); Set-Cookie dedup + drift-guard oracle realign, middleware rewrite locale propagation onto the visible URL, and `x-nextjs-matched-path` on static `_next/data` responses (brrrd runtime). Verified via `harness:harvest --fixtures test/e2e/middleware-rewrites/test/index.test.ts --bundlers webpack --name close-middleware-rewrites`.

**2026-06-23 — `test/e2e/app-dir/app-basepath` closed (webpack).** 3 failing assertions → 0 (13 pass). Server Action `redirect()` on a non-dynamic prerendered App Router page now reaches the co-located Page handler: the brrrd router skips the Prerender artifact for `next-action` POSTs (prerender route_ids aren't registered with the Node bridge) so matching falls through to the registered Page entry, whose handler runs the action and emits 303 + `x-action-redirect` (brrrd runtime). Verified via `harness:harvest --fixtures test/e2e/app-dir/app-basepath/index.test.ts --bundlers webpack --name close-app-basepath`.

**2026-06-23 — `test/e2e/getserversideprops` closed (webpack).** 48 pass / 48 (previously failing with 500s / custom-500 / notFound). No new change — resolved by the earlier i18n dynamic data-route + auto-static dynamic `_next/data` + locale routing fixes propagating to Pages-Router GSSP. Verified via `harness:harvest --fixtures test/e2e/getserversideprops/test/index.test.ts --bundlers webpack --name close-getserversideprops`.

**2026-06-23 — `test/e2e/app-dir/resume-data-cache` closed (webpack).** 5 pass / 5 (previously failing on PPR resume-data-cache RSC value consistency). No new change — resolved by earlier routing/PPR fixes. Verified via `harness:harvest --fixtures test/e2e/app-dir/resume-data-cache/resume-data-cache.test.ts --bundlers webpack --name close-resume-data-cache`.

**2026-06-23 — `test/e2e/app-dir/parallel-routes-revalidation` closed (webpack).** 16 pass / 1 skip / 17. No new change — resolved by earlier routing/PPR fixes. Verified via `harness:harvest --fixtures test/e2e/app-dir/parallel-routes-revalidation/parallel-routes-revalidation.test.ts --bundlers webpack --name close-parallel-routes-revalidation`.

## Status

Initial local harness integration exists. `scripts/local-harness.mjs` runs a checked-out Next canary test fixture or
group against the local brrrd deploy scripts, so failures can be reproduced before waiting for GitHub Actions. The
runner now follows the Next checkout `.node-version` by default, using `npx -y node@<version>` when the current shell
Node does not match, so local results do not drift from the CI Node 20 baseline. Use `--node-version current` only when
intentionally probing Node-version-specific Next behavior. The CI workflow still runs deploy-test shards manually or on
schedule. The current green shards are a compatibility signal, not the full official suite. The workflow makes the
bundler axis explicit: `webpack` is the forced webpack baseline, `turbopack` is tracked separately, and `next-default`
records whatever the checked-out Next canary uses when no bundler flag is supplied.

The local loop is now harvest-first. Before changing production adapter/runtime code for a new conformance push, run
several shard/bundler targets through `scripts/harness-harvest.mjs` with bounded parallelism, then classify the results
by root-cause bucket instead of fixture name. Prefer `--expand-fixtures` for broad inventory: it asks Next
`run-tests.js --print-tests` for each shard, then schedules the individual fixtures directly so one slow fixture does
not hold the entire shard hostage. Single-fixture debugging should start only after the failure inventory identifies the
highest fan-out bucket and representative fixtures. The harvest runner writes `harvest-plan.json`,
`harvest-partial.json`, `harvest-summary.json`, fixture-list metadata, and per-target stdout/stderr logs; slow targets
are recorded separately so long fixtures can be split into a slow bucket. Because Next `run-tests.js` writes fixture
result JSON files inside the shared Next checkout, the harvest scheduler serializes targets that can write the same
result file: group targets serialize by shard group, while expanded fixture targets serialize only by fixture path.

Local webpack shards `1/64` through `12/64` now pass on the official Node version. Forced Turbopack shards
`2/64`, `3/64`, `4/64`, `8/64`, and `9/64` also pass locally. Shards `2/64`, `3/64`, and `9/64` pass locally on `next-default`; shards `4/64` and `8/64`
have targeted feature coverage on `next-default` but are classified as `next-assumption` because Webpack-specific Next
fixtures still key their skip/assertion paths off `IS_TURBOPACK_TEST` while the `next-default` axis intentionally leaves
explicit bundler test flags unset. Shard `18/64` is green in the official workflow for both webpack and forced
Turbopack. Shard `8/64` covers App prerender response metadata (`.meta` status/headers) and encoded static prerender
paths; shard `9/64` now passes locally on webpack, forced Turbopack, and next-default, covering App Router RSC data
static serving, default-locale i18n rewrite semantics, worker WASM packaging, and metadata static files. Shard `2/64` covers middleware request header override propagation into Serverless
Functions, Edge Functions, and `next/headers`, draft mode cookies from middleware, dynamic static route precedence, and
Pages/App smoke regressions such as custom 404, router readiness, and image-from-node-modules. Shard `3/64` covers i18n
default-locale redirects, locale-aware middleware matchers, global error handling, PPR smoke, first-class `/_next/image`
serving for local static images, trailing-slash redirect query preservation, and Pages Router readiness. Shard `4/64` covers non-i18n middleware custom matchers,
middleware rewrites to Pages Router index handlers, metadata static-file GSP, router prefetch, and worker webpack smoke.
Shard `6/64` covers App Router instant prefetch/PPR staged rendering cleanup, metadata static files under route groups,
catch-all parallel routes, i18n redirect source-locale handling with basePath, and similar Pages/App path precedence.
Shard `7/64` covers App Router prefetch request priority/test-mode parity, generated App not-found fallback routing,
metadata static-file intercepting routes, Pages basePath data/static source lookup, and i18n redirect locale-disabled
semantics. Shards `10/64`, `11/64`, and `12/64` cover App root params/generateStaticParams cache headers, i18n
preferred locale detection, server asset module runtime chunk loading, middleware/proxy follow-up coverage, metadata
static-file routing, dynamic route interpolation, and App/Pages regression smoke.
The next webpack frontier is being handled with 16-shard fixture-expanded harvest batches. The first `17/64` through
`32/64` harvest completed with 256 fixture targets and 36 failures before fixes; its artifact root is
`/Users/ggm/work/.brrrd-local-harness/2026-06-21T02-05-28-068Z-webpack-frontier-17-32-batch`.

The workflow separates build and test jobs. It builds the adapter, brrrd runtime, and Next.js checkout once, saves that
workspace, and fans out the requested test shards from the saved workspace. `group=1/64` runs one smoke shard,
`group=1/64,2/64` runs an explicit subset, and `group=all` expands to all 64 shards for the selected bundler axis. The
build/test split itself has been smoke-validated on all three bundler axes.

## Failure categories

- `pass`: passes on the local brrrd deploy harness.
- `adapter-bug`: `@brrrd/adapter` build-output compiler, packaging, or manifest issue.
- `runtime-bug`: brrrd runtime routing, cache, Node compatibility, or static serving issue.
- `fleet-required`: local brrrd is not enough; the test needs live deploy/log/cleanup semantics.
- `next-assumption`: the test assumes Vercel/fleet-like infrastructure directly. Link an upstream issue when possible.
- `unsupported-yet`: valid Next feature that brrrd intentionally has not implemented yet.
- `flaky-infra`: timeout, port leak, browser install, or harness infrastructure issue.

## Current matrix

| Scope | Status | Notes |
| --- | --- | --- |
| Local deploy script smoke | pass | 2026-06-19: copied `brrrd/examples/nextjs-basic` to a temp app, ran `deploy -> curl / -> logs -> cleanup -> cleanup`; `/` returned 200 and required log markers were present. |
| Local official harness runner | pass | 2026-06-19/20: `npm run harness:fixture` and `npm run harness:group` wrap `/Users/ggm/work/next.js` without modifying Next fixture sources. The runner writes `local-harness.json`, stdout/stderr, result JSON, and copied build/runtime manifests under `/Users/ggm/work/.brrrd-local-harness/<run>`. It also sets `TMPDIR`/`TEMP`/`TMP` inside the run artifact directory, avoiding macOS temp-volume `ENOSPC` during shard execution. The child process now runs in its own process group with a hard timeout (`--timeout-ms` / `BRRRD_LOCAL_HARNESS_TIMEOUT_MS`), so stuck Jest/Playwright descendants are terminated and recorded instead of blocking local debugging indefinitely. For deep local debugging, `BRRRD_HARNESS_COPY_PACKAGE=1` makes `e2e-logs.sh` persist a runnable `dist/brrrd-package` copy in diagnostics; this is opt-in so normal CI artifacts stay small. |
| Local official harvest runner | pass | 2026-06-20: `npm run harness:harvest -- --groups 6/64,7/64 --bundlers webpack,next-default --parallel 2 --dry-run` planned the expected group matrix, and `--expand-fixtures` now expands each shard through Next `run-tests.js --print-tests` before scheduling individual fixture targets. Adapter tests cover argument parsing, fixture-list parsing, fixture expansion, root-cause bucket suggestions, bucketed summaries, slow target reporting, same-group scheduler conflict avoidance, and same-fixture cross-bundler conflict avoidance. `scripts/local-harness.mjs` no longer deletes Next `*.results.json` by default, preventing parallel harvest jobs from racing; serial cleanup remains available through `--clean-results`. `stdout.log` and `stderr.log` are streamed during execution rather than appearing only after a target exits. `local-harness` and `harness-harvest` now terminate child process groups on SIGINT/SIGTERM so interrupted harvests do not leave run-tests, brrrd, or Playwright processes behind. |
| Local official fixture-expanded harvest `7/64` webpack | pass | 2026-06-20 local run `npm run harness:harvest -- --groups 7/64 --bundlers webpack --parallel 3 --name fixture-harvest-webpack-7 --expand-fixtures --timeout-ms 900000 --slow-ms 300000` wrote `/Users/ggm/work/.brrrd-local-harness/2026-06-20T16-11-28-193Z-fixture-harvest-webpack-7/harvest-summary.json`. It expanded one shard into 17 fixture targets and completed in about 6 minutes with 14 pass / 3 fail. Follow-up log analysis split the red fixtures into three buckets: App Router RSC prefetch/cache/URL/priority semantics (`app-prefetch/prefetching`), App not-found fallback for unmatched paths (`prefetching-not-found` rendered generic `Not Found` instead of the app `_not-found` document), and Pages data route packaging (`middleware-dynamic-basepath-matcher`). 2026-06-20 follow-up: the Pages data route packaging bucket is fixed for the representative fixture. Adapter source lookup now strips `basePath` only when reading `.next/server/pages` prerender HTML/data files while preserving the public mount path, and `middleware-dynamic-basepath-matcher` passes locally with `verify-middleware-dynamic-basepath-data-source`. Regression coverage: `onBuildComplete resolves Pages Router basePath prerender HTML from unprefixed server/pages` and `onBuildComplete resolves Pages Router basePath prerender data JSON from unprefixed server/pages`. A later representative `app-prefetch/prefetching.test.ts` run first narrowed the App prefetch bucket from runtime 500/PPR failure to fetch-priority fidelity only, then `verify-app-prefetch-test-mode-env` passed 17/17 with 1 skipped. Root cause: local custom deploy did not provide the Vercel deploy build env `NEXT_PRIVATE_TEST_MODE=e2e`, so Next's fixture-injected `__NEXT_TEST_MODE` alias was absent and the client did not send `Next-Test-Fetch-Priority`; `scripts/local-harness.mjs` and `scripts/e2e-deploy.sh` now set that test-mode build env, plus `VERCEL_NEXT_BUNDLED_SERVER=1`, by default. Regression coverage: `test/local-harness.test.mjs` asserts the deploy env, and the official `app-prefetch/prefetching.test.ts` fixture passed at `/Users/ggm/work/.brrrd-local-harness/2026-06-20T21-21-20-760Z-verify-app-prefetch-test-mode-env`. The final App not-found fallback bucket was a runtime routing gap: unmatched App Router paths should rematch the generated `/_not-found` document, and RSC no-match requests should rematch `/_not-found.rsc`, with a 404 status override while preserving the original invocation URL. brrrd now models those as first-class custom-not-found candidates after `/404`. Regression coverage: `no_match_uses_app_not_found_fallback_with_not_found_status_override`, `rsc_no_match_uses_app_not_found_flight_fallback`, the representative fixture `verify-prefetching-not-found-app-fallback-rebuilt`, and full local `group=7/64` webpack at `/Users/ggm/work/.brrrd-local-harness/2026-06-20T21-35-37-809Z-shard-7-webpack-after-app-not-found-fallback` with all 17 fixtures passing on retry 0. |
| Local official fixture-expanded harvest `6/64,10/64,11/64,12/64` webpack | pass | 2026-06-20 local run `npm run harness:harvest -- --groups 6/64,10/64,11/64,12/64 --bundlers webpack --parallel 4 --name fixture-harvest-webpack-6-10-12 --expand-fixtures --timeout-ms 900000 --slow-ms 180000` wrote `/Users/ggm/work/.brrrd-local-harness/2026-06-20T16-23-57-775Z-fixture-harvest-webpack-6-10-12/harvest-summary.json`. It expanded 4 shards into 65 fixture targets; corrected status-based inventory was 54 pass / 11 fail. Bucket inventory: `middleware/proxy` was the largest bucket with 5 fixtures (`proxy-nfc-traced`, `proxy-runtime-nodejs`, `middleware-general`, `middleware-general/node-runtime`, `middleware-redirects`), followed by App Router prefetch/PPR with 2 fixtures (`prefetch-true-instant`, `concurrent-navigations/mismatching-prefetch`), plus single-fixture buckets for App root params (`app-root-params-getters/generate-static-params`), server asset modules, i18n preferred locale detection, and dynamic route interpolation. Slow failed fixtures were all middleware/proxy (`middleware-general`, `middleware-general/node-runtime`, `middleware-redirects`). Follow-up batches cleared these buckets with representative fixtures and low-level regressions: Node proxy/middleware compiler/runtime CJS loading, middleware request/data normalization, Pages basePath data source lookup, App prefetch/PPR abort cleanup, App not-found fallback rematching, prerender HTML cache headers, runtime chunk aliases for server asset modules, and preferred-locale i18n root redirects. Current group-level revalidation passes for `6/64`, `7/64`, `10/64`, `11/64`, and `12/64` on local webpack. |
| Local official groups `10/64`, `11/64`, `12/64` webpack after App root/i18n/server-asset batch | pass | 2026-06-21 local group reruns passed all selected fixtures on retry 0: `verify-webpack-10-after-root-params`, `verify-webpack-11-after-i18n-assets`, and `verify-webpack-12-after-10-11-batch`. Root causes fixed: prerender HTML responses lacked `x-nextjs-cache: HIT`; `.next/server/chunks/**` needed a `/bundle/chunks/**` runtime alias for `__dirname + "/chunks"` module loading; brrrd runtime lacked Next preferred-locale root redirect semantics and the manifest had no `i18n.localeDetection` schema field. Regression coverage: brrrd `html_prerender_static_responses_carry_cache_header`, `i18n_preferred_locale_*`, adapter `compileRouting preserves i18n localeDetection false`, adapter server chunk alias fail-fast coverage, and official fixtures `app-root-params-getters/generate-static-params`, `i18n-preferred-locale-detection`, and `server-asset-modules`. |
| Local official fixture-expanded harvest `17/64-32/64` webpack batch 1 | adapter-bug/runtime-bug | 2026-06-21 local run `harness:harvest -- --groups 17/64,...,32/64 --bundlers webpack --parallel 4 --list-parallel 4 --expand-fixtures --capture-context --timeout-ms 1800000 --slow-ms 180000 --defer-after-ms 300000 --max-deferred-running 2 --cleanup-target-artifacts passed --name webpack-frontier-17-32-batch` expanded 16 shards into 256 fixture targets and finished with 36 failures. Bucket inventory before fixes: `unclassified` 12, `PPR/instant prefetch` 6, `App not-found fallback` 4, `middleware/proxy` 4, `bundler output shape` 3, `Pages data route` 2, `flaky infra` 2, plus single-fixture `RSC prefetch cache semantics`, `i18n/basePath`, and `metadata/static asset`. Follow-up fixed two representative failures: `actions-unrecognized` was reclassified from PPR to `bundler output shape` and now passes after the adapter's scoped Next server source-map compatibility policy treats `.next/server/**/*.map` as non-executable server artifacts without globally emptying user-authored `.map` imports; `segment-cache/deployment-skew` now passes after `NEXT_DEPLOYMENT_ID` is recorded in `manifest.build.deploymentId` and brrrd serves static/RSC responses from service deployment metadata. Regression coverage: adapter `bundleAppHandler excludes server source maps from webpack dynamic chunk require contexts`, adapter `bundleAppHandler does not globally empty user-authored source map imports`, adapter `writeManifest records NEXT_DEPLOYMENT_ID as build metadata`, harvest bucket classifier test, brrrd `static_response_deployment_id_prefers_service_metadata`, brrrd `service_descriptor_preserves_next_deployment_id`, full `npm --prefix /Users/ggm/work/brrrd-adapter test`, full `cargo test -p brrrd --lib`, and the representative official fixtures `actions-unrecognized` and `segment-cache/deployment-skew`. A second same-window harvest snapshot at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T03-26-16-554Z-webpack-frontier-17-32-after-ppr-template-fix/harvest-snapshot-before-fix.json` captured 255/256 fixture results before stopping the deferred tail: 32 failures, 4 slow targets, and 1 deferred target. That run narrowed the remaining PPR/static-shell class to PPR prerender document requests incorrectly serving raw `.next/server/app/*.html` without the Next handler's resume/bootstrap injection. Adapter now marks PPR prerender routes with route-level `ppr: true`, and brrrd routing skips prerender filesystem serving for those document requests so the generated app-page handler can inject `__NEXT_CLIENT_RESUME` / instant-navigation bootstrap. Regression coverage: adapter `PPR app prerenders are marked so runtime can route document requests through handler`, brrrd `ppr_document_request_uses_page_handler_instead_of_static_shell`, and representative official fixtures `segment-cache/basic` plus `segment-cache/encoded-slash-params` passing locally on webpack. Remaining failures in this harvest are still bucketed but not all green-classified: `unclassified`, `middleware/proxy`, `App not-found fallback`, `Pages data route`, `bundler output shape`, `flaky infra`, `RSC prefetch cache semantics`, and `i18n/basePath` need the next fan-out batch before another broad conformance claim. |
| Local official fixture-expanded harvest `33/64-48/64` webpack batch 1 | adapter-bug/runtime-bug | 2026-06-21 local run `harness:harvest -- --groups 33/64,...,48/64 --bundlers webpack --parallel 4 --list-parallel 4 --expand-fixtures --capture-context --timeout-ms 1800000 --slow-ms 180000 --defer-after-ms 300000 --max-deferred-running 2 --cleanup-target-artifacts passed --name webpack-frontier-33-48-batch` expanded 16 shards into 256 fixture targets and completed in about 45 minutes. Artifact root: `/Users/ggm/work/.brrrd-local-harness/2026-06-21T07-06-29-406Z-webpack-frontier-33-48-batch`; summary: `harvest-summary.json`. Result: 223 pass / 33 fail, with 6 slow targets and no deferred target left running. Automatic buckets are noisy (`unclassified` 17), so manual inventory is the source of truth for the next fix batch: App Router Edge ALS/workStore context loss (`draft-mode`, `app-action-node-middleware`, `app-action-progressive-enhancement`, `import-conditions`, `app-action`); PPR/intercept/segment prefetch semantics (`interception-route-prefetch-cache`, `parallel-routes-root-param-dynamic-child`, `prefetch-inlining*`, `prefetch-runtime`); App static prerender artifact modeling (`chrome-devtools-workspace-base-path`, `app-basepath`, `next-form/basepath`); Pages static props/data routes (`fallback-route-params`, `preview-fallback`, `prerender.test`); bundler/package asset resolution (`prerender-native-module`, `twoslash`, `scss/catch-all-module`) plus conditional exports externalization (`esm-externals`); segment-cache/not-found empty handler errors (`back-forward-cache`, `prefetch-inlining`); trailingSlash canonical behavior (`trailing-slashes/with-trailing-slash`, `trailing-slashes/without-trailing-slash`); metadata/use-cache route handler semantics (`use-cache-metadata-route-handler`, `use-cache-route-handler-only`); Edge route params/rewrite (`edge-route-catchall`, `edge-route-rewrite`); and one-off follow-ups for `tty` compatibility, `next-after`/waitUntil semantics, `proxy-request-with-middleware`, `next-dynamic-csp-nonce`, `prerender-preview`, and `error-handler-not-found-req-url`. The highest fan-out bucket is App Router Edge ALS/workStore. Initial low-level brrrd regressions added for async callback args, nested ALS across await, precreated/deferred promises, Web Streams consumed outside `run`, WebCrypto, `process.nextTick`, queueMicrotask-to-nextTick, and async `AsyncLocalStorage.bind`; all pass, so the remaining reduction should target the actual Next edge/React render boundary rather than basic ALS primitives. This row is inventory only; no production fix is claimed. |
| Local official groups `35/64`, `36/64`, `48/64` webpack after routing/static/CSP batches | pass | 2026-06-21 follow-up reruns turned three shards from the `33/64-48/64` inventory green on local webpack. `35/64` and `36/64` passed as `webpack-35-36-after-pages-render404-and-pages-static` at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T13-28-08-677Z-webpack-35-36-after-pages-render404-and-pages-static`, confirming the Pages render404/error URL, dynamic static HTML content-type, conditional exports, and trailing-slash canonical behavior buckets for those shards. `48/64` passed as `webpack-48-after-middleware-response-header-propagation` at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T13-36-57-976Z-webpack-48-after-middleware-response-header-propagation`. Root cause for the remaining shard-48 red (`next-dynamic-csp-nonce`) was middleware response headers not being mirrored into downstream request headers before App render reads the CSP nonce; brrrd now follows Next's proxy/middleware resolution semantics for continue/rewrite actions. Regression coverage: JS middleware bridge `proxy phase mirrors forwarded response headers into downstream request headers`, updated proxy action request-header expectations, `node --test crates/brrrd/src/js/node/middleware_bridge.test.mjs`, `cargo build -p brrrd`, representative official fixture `next-dynamic-csp-nonce-after-middleware-header-propagation`, and full group `48/64` webpack. Classification: semantic runtime fix, not a compatibility shim. |
| Local official groups `33/64-48/64` webpack after OTel/PPR document fixes | adapter-bug/runtime-bug | 2026-06-21 group harvest `harness:harvest -- --groups 33/64,...,48/64 --bundlers webpack --parallel 4 --concurrency 1 --defer-after-ms 300000 --max-deferred-running 2 --slow-ms 300000 --capture-context --brrrd-bin /Users/ggm/work/brrrd/target/debug/brrrd --name webpack-33-48-after-ppr-otel` completed at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T14-38-53-502Z-webpack-33-48-after-ppr-otel`. The prior representative reds are fixed: `cache-components-allow-otel-spans` passes after the dispatcher prefers a packaged `@opentelemetry/api` runtime file over the fallback stub and brrrd provides the needed `node:tty`/`node:perf_hooks` surface; `back-forward-cache` passes after PPR document requests route through the generated app handler instead of serving raw static shells. The broader 16-shard window is not green: groups `33/64`, `35/64`, `36/64`, `37/64`, `40/64`, and `48/64` passed; groups `34/64`, `38/64`, `39/64`, `41/64`, `42/64`, `43/64`, `44/64`, `45/64`, `46/64`, and `47/64` failed with 18 fixture failures. Updated harvest bucketing now classifies 11 failures as `segment-cache/PPR prefetch` (`segment-cache/metadata`, optimistic routing, prefetch-auto, prefetch-inlining*, interception route prefetch cache, parallel route dynamic child, prefetch app shell, partial RSC, and partial-prefetching-config), plus `bundler output shape` (`prerender-native-module`), `metadata/static asset` (`use-cache-metadata-route-handler`), and five unclassified follow-ups (`next-after-app-deploy`, `prerender-crawler`, `twoslash`, `prerender.test`, `use-cache-route-handler-only`). Next action: reduce the `segment-cache/PPR prefetch` bucket with representative fixtures and low-level routing/runtime regressions before claiming more green shards. |
| Local official group `41/64` webpack after static-path/PPR/crawler fixes | pass | 2026-06-21 local rerun `harness:group -- --group 41/64 --bundler webpack --name webpack-41-after-static-paths-crawler --concurrency 1 --timeout-ms 1800000 --capture-context --brrrd-bin /Users/ggm/work/brrrd/target/debug/brrrd` passed all 16 selected fixtures on retry 0. Artifact root: `/Users/ggm/work/.brrrd-local-harness/2026-06-21T16-44-08-876Z-webpack-41-after-static-paths-crawler`. This clears only shard `41/64`, not the full `33/64-48/64` window. Representative fixes verified first: `interception-route-prefetch-cache` passes after intercepted RSC prefetches are downgraded before the node handler and RSC navigation responses get deployment-id headers; `segment-cache/metadata` and `segment-cache/prefetch-auto` pass after PPR fallback metadata is preserved and full RSC requests are resumed with `next-resume`; `parallel-routes-root-param-dynamic-child` passes after App Router `fallback: false`/`generateStaticParams` dynamic routes are emitted as `staticPathsOnly` for both HTML and RSC handler routes while preserving Next bypass conditions; `prerender-crawler` passes after Pages Router dynamic SSG fallback shell routes are marked as `pagesFallbackShell` and brrrd skips those shells for known bot user agents so the handler can do blocking crawler rendering. Regression coverage: adapter `fallback false App SSG dynamic routes are static-path-only with bypass conditions`, adapter `extractDynamicPrerenderRoutes preserves Next fallback modes`, adapter Pages fallback shell tests, brrrd `static_paths_only_dynamic_route_requires_bypass_condition`, brrrd `pages_fallback_shell_is_skipped_for_bot_requests`, plus the representative official fixtures and full `npm --prefix /Users/ggm/work/brrrd-adapter test`. |
| Local official fixtures `app-action-node-middleware` and `app-action` webpack after Server Actions redirect batches | pass | 2026-06-21 two representative fixtures from the `33/64` and `37/64` Actions buckets now pass on local webpack. First, brrrd request ingress defaults `x-forwarded-host`, `x-forwarded-proto`, and `x-forwarded-port` from the visible `Host` header, matching Next's base-server assumptions and preserving Server Actions CSRF host/origin validation without weakening the check. Second, adapter filtering now treats Next's Pages Router `.next/server/rsc-fallback.json` static output as a non-public fallback artifact rather than an App Router RSC data route, so Server Action redirects to Pages routes receive an invalid RSC response and correctly fall back to a full document navigation. Regression coverage: brrrd Rust `forwarded_headers_default_from_visible_host`, `forwarded_headers_preserve_existing_proxy_values`, `forwarded_port_parses_ipv6_authority`, adapter `Pages Router RSC fallback static outputs are not published as RSC data routes`, adapter `onBuildComplete does not publish Pages Router rsc-fallback as RSC data`, `npm --prefix /Users/ggm/work/brrrd-adapter test -- test/routing.test.mjs test/fail-fast.test.mjs`, `cargo test -p brrrd forwarded_`, and `cargo build -p brrrd`. Official fixture artifacts: `/Users/ggm/work/.brrrd-local-harness/2026-06-21T13-56-07-002Z-app-action-node-middleware-after-pages-rsc-fallback-filter` and `/Users/ggm/work/.brrrd-local-harness/2026-06-21T13-59-11-417Z-app-action-after-pages-rsc-fallback-filter`, both 75 passed / 3 skipped. Classification: semantic adapter/runtime fixes. This clears representative Actions failures, but does not claim full `33/64` or `37/64` shard green because other buckets in those shards remain. |
| Local official fixture-expanded harvest `49/64-64/64` webpack batch 1 | adapter-bug/runtime-bug | 2026-06-21 fresh local run `harness:harvest -- --groups 49/64,...,64/64 --bundlers webpack --parallel 4 --list-parallel 4 --expand-fixtures --capture-context --timeout-ms 1800000 --slow-ms 180000 --defer-after-ms 300000 --max-deferred-running 2 --cleanup-target-artifacts passed --name webpack-frontier-49-64-current` expanded 16 shards into 256 fixture targets. Artifact root: `/Users/ggm/work/.brrrd-local-harness/2026-06-21T05-48-48-992Z-webpack-frontier-49-64-current`; summary: `harvest-summary.json`. Result: 235 pass / 21 fail in about 34 minutes. Slow targets: `fallback-shells`, `server-actions-redirect-middleware-rewrite`, and deferred long-tail `segment-cache/vary-params-base-dynamic`, which eventually failed after about 20 minutes without blocking the rest of the harvest. A previous `webpack-frontier-49-64-after-lazy-edge` run is explicitly discarded because production adapter code changed while it was running. Manual inventory for the next batch: metadata route/runtime semantics (`metadata-dynamic-routes`, `metadata-edge`, `metadata-font`), legacy image optimizer parity (`next-image-legacy/default`, `next-image-legacy/unicode`), PPR/segment prefetch and fallback shell semantics (`react-version`, `fallback-shells`, `segment-cache/vary-params-base-dynamic`), basePath invalid static asset/custom-not-found source modeling (`invalid-static-asset-404-app-base-path`, `invalid-static-asset-404-pages-base-path`), Pages router dynamic/SSR/data semantics (`getinitialprops`, `getserversideprops`), middleware/proxy with basePath and Server Actions (`middleware-base-path`, `server-actions-redirect-middleware-rewrite`), rewrite/client/API navigation parity (`rewrites-manual-href-as`, `link-with-api-rewrite`), ISR/revalidate reason (`revalidate-reason`), Node/runtime fetch compatibility (`undici-fetch`), bundler/runtime dependency packaging (`handle-non-hoisted-swc-helpers`), i18n API support (`i18n-api-support`), and cache tag encoding/runtime behavior (`non-ascii-cache-tags`). This row is inventory only; choose the highest fan-out bucket and reduce it to representative low-level regressions before production fixes. |
| Local official fixture-expanded harvest `49/64-64/64` webpack batch 2 | adapter-bug/runtime-bug | 2026-06-22 rerun `harness:harvest -- --groups 49/64,...,64/64 --bundlers webpack --parallel 4 --list-parallel 4 --expand-fixtures --capture-context --timeout-ms 1800000 --slow-ms 180000 --defer-after-ms 300000 --max-deferred-running 5 --cleanup-target-artifacts passed --brrrd-bin /Users/ggm/work/brrrd/target/debug/brrrd --name webpack-49-64-harvest` expanded 16 shards into 256 fixture targets. Artifact root: `/Users/ggm/work/.brrrd-local-harness/2026-06-22T01-03-33-668Z-webpack-49-64-harvest`; result: 241 pass / 15 fail. The persistent ledger was updated with 15 events; open failures are tracked in `docs/next-harness-failure-ledger.jsonl` and `docs/next-harness-failure-ledger.state.json`. The largest open bucket is still `segment-cache/PPR prefetch`. Follow-up semantic fix: dynamic App prerender segment data artifacts such as `/[teamSlug]/[project].segments/_tree.segment.rsc` now emit concrete dynamic static routes from the Next `prerender-manifest` route regex, before full-route RSC handlers. This is adapter compiler modeling, not a fixture/shard branch. Regression coverage: adapter `onBuildComplete maps dynamic PPR tree segment artifacts to concrete segment prefetch paths`, brrrd `segment_prefetch_request_uses_dynamic_prerender_segment_artifact`, full adapter test suite, `just lint`, and representative fixture manifest/network inspection for `segment-cache/vary-params-base-dynamic`. The representative fixture rerun was stopped after 5m30s as slow; manifest and network confirmed the new static routes and `200 text/x-component` / `x-nextjs-cache: HIT` segment responses, but this row does not claim the full shard window green. Next action: rerun affected PPR groups with defer-aware harvest and close ledger entries only after pass evidence. |
| Local official affected PPR direct fixture harvest after dynamic segment routes | adapter-bug/runtime-bug | 2026-06-22 direct fixture run `harness:harvest -- --fixtures <9 open PPR fixtures> --bundlers webpack --parallel 4 --capture-context --timeout-ms 1800000 --slow-ms 180000 --defer-after-ms 300000 --max-deferred-running 5 --cleanup-target-artifacts passed --brrrd-bin /Users/ggm/work/brrrd/target/debug/brrrd --name ppr-open-fixtures-after-deferred-exit-ledger` wrote `/Users/ggm/work/.brrrd-local-harness/2026-06-22T02-40-37-879Z-ppr-open-fixtures-after-deferred-exit-ledger/harvest-summary.json`. Result: 4 pass / 4 fail / 1 slow-deferred, with ledger events for all 9 fixtures. Closed as passing in that run: `segment-cache/metadata`, `segment-cache/prefetch-auto`, `segment-cache/prefetch-inlining-no-cache-components`, and `segment-cache/prefetch-partial-rsc`. At that point the remaining `segment-cache/PPR prefetch` work was `segment-cache/prefetch-inlining`, `segment-cache/vary-params`, `segment-cache/search-params`, `partial-prefetching-config`, and slow `segment-cache/vary-params-base-dynamic`; later rows record their closure. Harness infra follow-up in the same batch added direct `--fixtures` / `--fixture-list` harvest targets, persistent ledger closure from direct fixture passes, and deferred-only exit with process-group cleanup. Regression coverage: `test/harness-harvest.test.mjs` for direct targets, bounded deferred exit, and slow-fixture summaries; smoke run `deferred-exit-smoke` verified child process cleanup. |
| Local official fixture `partial-prefetching-config` webpack after PPR shell/resume-tail combine | pass | 2026-06-22 representative fixture `test/e2e/app-dir/partial-prefetching-config/partial-prefetching-config.test.ts` passed as `ppr-partial-prefetch-after-shell-tail-combine` at `/Users/ggm/work/.brrrd-local-harness/2026-06-22T03-20-22-993Z-ppr-partial-prefetch-after-shell-tail-combine`. Root cause: PPR document navigation needs both the concrete static shell containing postponed boundaries and the generated app handler's `next-resume` tail; serving only the shell or only the tail leaves dynamic content missing and can surface React error 412. brrrd now resolves the concrete prerender shell, dispatches the matching app handler with resume metadata, removes stale `content-length`, and streams shell bytes before the handler tail. Adapter follow-up in the same bucket orders concrete App prerender segment data routes before dynamic segment templates so static segment artifacts win over fallback shells. Regression coverage: brrrd `ppr_document_request_resolves_concrete_shell_for_resume_tail`, `ppr_resume_metadata_turns_document_request_into_resume_post`, `ppr_resume_metadata_does_not_replace_full_prefetch_requests`, adapter `concrete App prerender segment data wins over dynamic segment templates`, full adapter routing/fail-fast tests, focused `cargo test -p brrrd ppr_`, `cargo build -p brrrd`, and the official fixture. Ledger now marks `partial-prefetching-config` closed; remaining PPR open items are `prefetch-inlining`, `search-params`, `vary-params`, and slow `vary-params-base-dynamic`. Classification: semantic runtime/compiler fix, not a fixture branch. |
| Local official fixture `app-action-size-limit-invalid` webpack after dispatcher metadata batch | pass | 2026-06-21 representative Server Actions fixture `test/e2e/app-dir/actions/app-action-size-limit-invalid.test.ts` passed locally as `verify-server-actions-current-fresh` and again after source-map policy narrowing as `verify-server-actions-after-map-policy`, both with 5/5 assertions green. Root cause was adapter dispatcher request metadata fidelity: Next App Router action handling expects a virtual project/dist request context, so the dispatcher now preserves incoming request meta and supplies `relativeProjectDir: "."`, `distDir: "/bundle/.next"`, and `hostname`. Regression coverage: adapter `dispatcher provides Next virtual project and dist request metadata` plus the official fixture artifacts at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T05-38-43-234Z-verify-server-actions-current-fresh` and `/Users/ggm/work/.brrrd-local-harness/2026-06-21T05-45-37-931Z-verify-server-actions-after-map-policy`. This does not claim the whole `33/64-48/64` Server Actions/action-middleware bucket is green; `app-action-form-state-node-middleware` and `next-form/basepath` remain inventory until representative reruns pass. |
| Local official fixtures `edge-can-read-request-body` and `middleware-static-files` webpack after proxy body/decoded matcher batch | pass | 2026-06-21 follow-up on the `17/64-32/64` middleware/proxy bucket split two semantic runtime gaps from the remaining data-request rewrite work. Proxy/middleware phase now buffers a matched request body once and replays it both to the middleware/proxy Web `Request` and the downstream Node route, so `edge-can-read-request-body` passes locally as `middleware-body-replay` with all 5 body formats green. Middleware matcher evaluation now considers the raw pathname and a valid percent-decoded pathname candidate, matching Next behavior for static files such as `/vercel%20copy.svg`, so `middleware-static-files` passes locally as `middleware-decoded-matchers` with all 19 assertions green. Regression coverage: brrrd Rust `proxy_phase_envelope_carries_replay_body`, brrrd Rust `matchers_consider_percent_decoded_path_candidates`, JS bridge `proxy phase middleware can read POST request body`, JS bridge `middleware matcher checks percent-decoded path candidates`, `node --test crates/brrrd/src/js/node/middleware_bridge.test.mjs`, `cargo check -p brrrd`, `cargo build -p brrrd`, and the two representative official fixtures. The remaining same-area red in `middleware-trailing-slash` is a separate `middleware/proxy data-request rewrite/trailingSlash normalization` sub-bucket, not covered by this batch. `app-action-form-state-node-middleware` was interrupted after exceeding the interactive window and remains slow/deferred inventory, not a conformance score. |
| Local official fixture `middleware-general/test/index.test.ts` webpack after Node proxy batch | runtime-bug | 2026-06-20 local run `npm run harness:fixture -- --fixture test/e2e/middleware-general/test/index.test.ts --bundler webpack --name verify-middleware-general-after-node-proxy --timeout-ms 900000` still failed after about 8.5 minutes: 37 pass / 29 fail. This confirms the small Node proxy compiler/CJS loader fix does not clear the slow general suite. The failure pattern is a separate runtime semantics bucket: rewrite rematch/route precedence (`/rewrite-1`, `/rewrite-3`, `/rewrite-to-config-rewrite` often resolves to the exported `pages/[id]` route instead of the intended rewrite target), malformed path decode handling (`/%2` returns 200/404 instead of 400), middleware env visibility, NextRequest URL/locale normalization, data-request rewrites, and shallow navigation request accounting. A low-level adapter regression was added for one suspected subcase, `Pages Router dynamic data outputs keep the public page handler pattern`, but the follow-up official fixture `verify-middleware-general-after-dynamic-data-handler` still failed at 35 pass / 31 fail. The real fixture manifest still emitted `blog-[slug]` with a data-route regex, so this bucket remains unresolved and should be reduced with preserved app diagnostics or a more faithful Adapter API context sample before more production edits. |
| Local official fixture `middleware-general/test/index.test.ts` webpack after request normalization / Pages data / prefetch-skip batch | pass | 2026-06-20 follow-up local runs narrowed and then cleared this representative slow fixture. `verify-middleware-general-after-data-i18n-nextconfig` failed at 56 pass / 10 fail after proxy phase data-request marking and manifest-driven middleware `nextConfig` fixed NextRequest URL/locale/data rewrite semantics; `verify-middleware-general-after-bad-path-decode` failed at 58 pass / 8 fail after brrrd rejected malformed request paths such as `/%2` before routing; `verify-middleware-general-after-abort-error` failed at 60 pass / 6 fail after brrrd normalized Deno fetch's default AbortError message to the Node/Web-compatible "The operation was aborted"; `verify-middleware-general-current-pages-data` failed at 62 pass / 4 fail after adapter Pages data rewrite aliases fixed client rewrite parity. Final local run `verify-middleware-general-after-prefetch-skip` passed 66/66 in about 291s. Root causes were runtime request/data normalization, adapter Pages data rewrite aliasing, middleware throw-to-500 handling for failed data requests, and Next-compatible non-SSG Pages data middleware prefetch skip (`x-middleware-skip`, `x-nextjs-matched-path`, `{}` body). Regression coverage: `npm --prefix /Users/ggm/work/brrrd-adapter test -- test/fail-fast.test.mjs`, `node --test crates/brrrd/src/js/node/middleware_bridge.test.mjs`, `cargo test -p brrrd middleware_pages_data_prefetch_bails_before_ssr_handler --lib`, `cargo test -p brrrd request_path_decode_failure_is_rejected_before_routing --lib`, `cargo test -p brrrd proxy_phase_only_marks_actual_next_data_urls --lib`, and the representative official fixture. |
| Local official harvest `6/64,7/64,8/64` webpack | pass | 2026-06-20 local harvest wrote `/Users/ggm/work/.brrrd-local-harness/2026-06-20T15-05-41-151Z-harvest-webpack-6-8/harvest-summary.json`: `8/64` passed, `6/64` failed only `prefetch-true-instant`, and `7/64` failed `app-prefetch`, `prefetching-not-found`, and `middleware-dynamic-basepath-matcher`. Bucket inventory: App Router prefetch/PPR semantics is the biggest bucket (`prefetch-true-instant`, `app-prefetch`, `prefetching-not-found`), while Pages Router static artifact/data modeling remains separate (`middleware-dynamic-basepath-matcher` missing `.next/server/pages/base/another-page.html`). 2026-06-20 follow-up: `prefetch-true-instant` now passes, and a full local `group=6/64` webpack rerun at `/Users/ggm/work/.brrrd-local-harness/2026-06-20T21-03-44-129Z-shard-6-webpack-after-prefetch-abort-cleanup` passed all 17 selected fixtures on retry 0. Root cause: Next/React staged App Router prefetch rendering uses a default Deno `AbortController.abort()` cleanup path that was surfacing as a fatal host event-loop error; brrrd now exposes the global object as an EventTarget-compatible Web global, suppresses expected default abort cleanup errors in the request loop, and treats only that exact Deno cleanup event-loop stack as non-fatal in the host loop. Regression coverage: brrrd `default_abort_cleanup_error_can_be_suppressed`, `loop_script_suppresses_next_abort_cleanup_errors`, `expected_abort_cleanup_event_loop_error_is_not_fatal`, `arbitrary_abort_error_still_remains_fatal`, `prefetch-true-instant` fixture, and `group=6/64` webpack. The App prefetch fetch-priority sub-bucket was reclassified as a harness deploy-env parity bug and fixed by setting `NEXT_PRIVATE_TEST_MODE=e2e` for local custom deploy builds; `app-prefetch/prefetching.test.ts` now passes. The App not-found fallback sub-bucket is fixed by runtime custom-not-found rematching for generated `/_not-found` and `/_not-found.rsc` routes. Full local `group=7/64` webpack now passes all 17 fixtures on retry 0. |
| Local official fixture `i18n-ignore-rewrite-source-locale` | pass | 2026-06-19/20: `npm run harness:fixture -- --fixture test/e2e/i18n-ignore-rewrite-source-locale/rewrites.test.ts --bundler webpack --name sample-i18n` originally reproduced the default-locale rewrite gap in about 18s: prefixed `/en`, `/sv`, `/nl` variants passed, while unprefixed `/rewrite-files/file.txt` and `/rewrite-api/hello` returned default-locale 404 HTML. 2026-06-20: after preserving rule-level `locale: false` and compiling i18n candidate semantics in brrrd runtime, `--name i18n-locale-false-fixed` passed 8/8. |
| Local official group `1/64` webpack | pass | 2026-06-19/20: `npm run harness:group -- --group 1/64 --bundler webpack --name smoke-group-1 --concurrency 1` selected 17 fixtures and passed locally in about 5 minutes using `/Users/ggm/work/next.js` canary `b216be9c`. |
| Local official group `2/64` webpack after middleware/Edge bridge fixes | pass | 2026-06-20 local run `npm run harness:group -- --group 2/64 --bundler webpack --name shard-2-webpack-middleware-fixed --concurrency 1` passed all 17 selected deploy fixtures on retry 0 under Next checkout `.node-version` Node 20. The prior red was `app-middleware`: dynamic static templates such as Pages auto-export `/[slug]` were incorrectly treated as filesystem phase and could beat exact App routes, Rust proxy phase marked middleware as already handled but the JS bridge reran middleware, middleware-generated `x-middleware-set-cookie` could be merged incorrectly with forged client sentinels, Edge functions received a Web `Headers` object where Next Edge adapter expects Node `OutgoingHttpHeaders`, and `draftMode().enable()` cookies emitted as regular `set-cookie` were filtered out. Regression coverage: brrrd route phase test, brrrd RSC dynamic static data route test, JS middleware sentinel/draft cookie tests, and Edge RequestData header-shape tests. |
| Local official group `2/64` Turbopack after middleware/Edge bridge fixes | pass | 2026-06-20 local run `npm run harness:group -- --group 2/64 --bundler turbopack --name shard-2-turbopack-current --concurrency 1` passed all 17 selected deploy fixtures on retry 0 under Next checkout `.node-version` Node 20. This proves the shard-2 middleware/proxy/header/draft-cookie/Edge bridge fixes are not webpack-only and still hold under Turbopack output shape. Regression coverage is the same low-level brrrd route/middleware/Edge bridge tests plus the forced Turbopack official shard. |
| Local official group `2/64` Next default after middleware/Edge bridge fixes | pass | 2026-06-20 local run `npm run harness:group -- --group 2/64 --bundler next-default --name shard-2-next-default-current --concurrency 1` passed all 17 selected deploy fixtures on retry 0 under Next checkout `.node-version` Node 20. Next canary built these fixtures with default Turbopack (`TURBOPACK=auto`) while both explicit bundler test flags stayed unset, so this shard now has evidence across webpack, forced Turbopack, and current canary default behavior. |
| Local official group `3/64` webpack after i18n/image matcher fixes | pass | 2026-06-20 local run `npm run harness:group -- --group 3/64 --bundler webpack --name shard-3-webpack-final-after-rebuild --concurrency 1` passed all 17 selected deploy fixtures on retry 0 under Next checkout `.node-version` Node 20. The previous reds were `i18n-default-locale-redirect`, `middleware-custom-matchers-i18n`, and `next-image-new/loader-config-default-loader-with-file`. Runtime now separates custom route rewrite/redirect i18n candidate semantics from middleware matcher i18n normalization: rewrite/redirect rules keep raw plus default-locale candidates, while locale-aware middleware evaluates the single internal pathname Next would see, so `/hello` matches as `/en/hello` and `/invalid/hello` does not. brrrd also serves `/_next/image` for local packaged static images without turning off Next image semantics. The latest source moves `/_next/image` out of the dispatch early-intercept path into a manifest `image-optimizer` filesystem route; runtime dispatch handles the resulting `RoutingOutcome::ImageOptimizer`. A related runtime redirect bug was fixed at the semantic layer: trailing-slash redirects now preserve the request query, so `/_next/image/?url=/logo.png&w=828&q=50` redirects to `/_next/image?url=/logo.png&w=828&q=50` instead of dropping optimizer parameters. Final-source fixture checks: `i18n-locale-false-after-rebuild` passed 8/8, `i18n-default-redirect-after-candidate-fix` passed 2/2, `image-loader-after-candidate-fix` passed 4/4, and `image-optimizer-route-pipeline-fixed` passed 4/4. Regression coverage: adapter default-locale handler/prerender alias tests, adapter `image-optimizer` route/basePath tests, middleware locale flag extraction test, brrrd middleware i18n matcher tests, brrrd routing locale false/default-locale tests, brrrd redirect query preservation test, and brrrd image optimizer response tests. |
| Local official group `3/64` Turbopack after i18n/image matcher fixes | pass | 2026-06-20 local run `npm run harness:group -- --group 3/64 --bundler turbopack --name shard-3-turbopack-current --concurrency 1` passed all 17 selected deploy fixtures on retry 0 under Next checkout `.node-version` Node 20. This confirms the shard-3 i18n matcher/default-locale redirect and default `/_next/image` local static image behavior is preserved under Turbopack output shape. |
| Local official group `3/64` Next default after i18n/image matcher fixes | pass | 2026-06-20 local run `npm run harness:group -- --group 3/64 --bundler next-default --name shard-3-next-default-current --concurrency 1` passed all 17 selected deploy fixtures on retry 0 under Next checkout `.node-version` Node 20. Next canary used its default Turbopack build path with explicit bundler test flags unset, so shard 3 now has evidence across webpack, forced Turbopack, and current canary default behavior. |
| Local official group `4/64` webpack after Pages index handler alias fix | pass | 2026-06-20 local run `npm run harness:group -- --group 4/64 --bundler webpack --name shard-4-webpack-pages-index-alias-fixed --concurrency 1 --timeout-ms 900000` passed all 17 selected deploy fixtures on retry 0 under Next checkout `.node-version` Node 20. The prior red was `middleware-custom-matchers`: middleware matched `/source-match` and `/has-match-*`, rewrote to `/`, but the adapter emitted the Pages Router executable index handler only at `^/index$`, so the JS middleware bridge could not rematch the rewrite destination and returned middleware-only 404. Adapter now keeps Next output `pathname`/id for dispatch while normalizing Pages Router handler `urlPath` from `/index` and `/nested/index` to `/` and `/nested` for public route patterns. Regression coverage: `test/routing.test.mjs` executable Pages Router index handler alias test plus the official fixture and shard. |
| Local official group `4/64` Turbopack after Pages index handler alias fix | pass | 2026-06-20 local run `npm run harness:group -- --group 4/64 --bundler turbopack --name shard-4-turbopack-pages-index-alias-fixed --concurrency 1 --timeout-ms 900000` passed all 17 selected deploy fixtures on retry 0. This confirms the Pages Router index handler public-path fix is part of the Adapter API build-output model and is not webpack-output-specific. |
| Local official group `4/64` Next default after Pages index handler alias fix | next-assumption | 2026-06-20 local run `npm run harness:group -- --group 4/64 --bundler next-default --name shard-4-next-default-current --concurrency 1 --timeout-ms 900000` passed the first 16 selected fixtures on retry 0, including `middleware-custom-matchers`, metadata static-file GSP, router prefetch, and image loader coverage. The only red was `worker-webpack5`, which is explicitly marked in Next source as a Webpack-specific test and skips only when `IS_TURBOPACK_TEST` is set. `next-default` intentionally leaves both explicit bundler test flags unset, but Next canary still built the fixture with default Turbopack (`TURBOPACK=auto`), so the Webpack-only fixture attempted to parse `faker@5.5.3` locale bytes under Turbopack and failed before adapter deploy. Forced `turbopack` passes the same shard because the fixture is skipped there. Owner: Next fixture/default-bundler assumption, not adapter/runtime. |
| Local official fixture `i18n-fallback-collision` webpack after dynamic prerender fallback compiler fix | pass | 2026-06-20 local run `npm run harness:fixture -- --fixture test/e2e/i18n-fallback-collision/i18n-fallback-collision.test.ts --bundler webpack --name i18n-fallback-collision-locale-handling-fixed --timeout-ms 900000` passed 11/11. Root cause: adapter treated every Pages Router dynamic output as an executable dynamic handler, even when Next `prerender-manifest.dynamicRoutes[page].fallback` was `false`; fallback:false SSG routes should expose only concrete prerendered filesystem paths and miss to 404. The same fixture also exposed i18n route normalization drift: an unprefixed default-locale dynamic handler could consume `/es/...` as ordinary params. Adapter now extracts dynamic prerender fallback modes from `prerender-manifest`, skips fallback:false dynamic Pages handlers, prefers `routes-manifest` `routeRegex` over Adapter API locale catch-all regexes for those dynamic pages, and marks unprefixed i18n dynamic handlers with `localeHandling: "unprefixed"`. brrrd runtime reads that route metadata and rejects configured locale prefixes for those handlers. Classification: semantic fix. Regression coverage: adapter `extractDynamicPrerenderRoutes preserves Next fallback modes`, adapter `fallback false Pages SSG dynamic routes are not emitted as executable handlers`, and brrrd `unprefixed_i18n_dynamic_route_rejects_locale_prefixed_paths`. |
| Local official group `5/64` webpack after segment prefetch and i18n fallback fixes | pass | 2026-06-20 local run `npm run harness:group -- --group 5/64 --bundler webpack --name shard-5-webpack-i18n-fallback-fixed --concurrency 1 --timeout-ms 1800000` passed all 17 selected deploy fixtures on retry 0. This shard now covers custom 404, App static prefetch, catch/global-error paths, metadata static-file dynamic route, nested not-found, prefetch search params, SCSS npm import, shallow routing, webpack loader env, disabled JS preload, i18n fallback collision, middleware custom matchers, image loader config, router rerender, and Yarn PnP MDX Pages. The prior red sequence was first `shallow-routing`, fixed by resolving `next-router-segment-prefetch` requests to `.segments/...segment.rsc` static artifacts before full-route RSC or dynamic handlers, then `i18n-fallback-collision`, fixed by the dynamic prerender fallback/i18n route metadata work above. Regression coverage: brrrd segment prefetch routing tests plus the adapter/runtime i18n fallback tests. |
| Local Next canary Pages Router webpack smoke | pass | 2026-06-19: built and served a temp `next@16.3.0-canary.58` Pages Router app with `next build --webpack`, local `@brrrd/adapter`, and `brrrd <dist/brrrd>`. This covers the previous `pages.runtime.prod.js -> require("critters")` build blocker and the static Pages Router `/index` output normalization; `GET /` returned 200. |
| Private runtime checkout | pass | 2026-06-19: configured `RUNTIME_REPO_DEPLOY_KEY` as a read-only deploy key for `burrr-ai/brrrd`; workflow now checks out runtime submodules recursively. |
| Official deploy suite `1/16` | flaky-infra | 2026-06-19 run `27823794521` reached `Run official deploy adapter tests` after adapter/runtime/Next builds, then stayed in that step for more than 15 minutes without available logs. It was cancelled and the workflow was updated to use a smaller `1/64` smoke shard plus timeout and diagnostics collection. |
| Official deploy suite `1/64` with Turbopack forced | unsupported-yet | 2026-06-19 run `27824888633` selected 17 tests, failed 11, and uploaded diagnostics. This was not a valid baseline because the workflow forced `IS_TURBOPACK_TEST=1`; observed failures include adapter compiler assumptions about `server/edge-runtime-webpack.js`, runtime 404/500 behavior, and Next/Turbopack fixture build errors such as `TypeError: y.get is not a function`. Turbopack coverage remains a separate axis. |
| Official deploy suite `1/64` with Next default bundler | unsupported-yet | 2026-06-19 run `27825901278` selected 17 tests and failed 11. This run did not set `IS_TURBOPACK_TEST`, but Next canary defaults an unqualified `next build` to Turbopack (`TURBOPACK=auto`), so it still was not the intended webpack baseline. The workflow now defaults to `IS_WEBPACK_TEST=1`; this run remains useful as `next-default` data. |
| Official deploy suite `1/64` webpack baseline | adapter-bug | 2026-06-19 run `27830591361` selected 17 tests at adapter `b67b093`: 9 passed and 8 failed. The previous adapter compile blocker (`pages.runtime.prod.js -> require("critters")`) is resolved; there are no `Could not resolve "critters"` failures. Remaining failures are now behavior/build issues: `404-page-app` receives an empty default 404 instead of the custom Pages Router 404, `cache-components` and `ppr-root-param-rsc-fallback` still fail during Next canary prerender/build with `TypeError: y.get is not a function`, `front-redirect-issue`, `metadata-navigation`, and `with-router` hit runtime 500/timeout behavior, `next-image-new/image-from-node-modules` misses the expected `srcset`, and `router-is-ready-app-gip` fails Pages Router runtime assertions. Diagnostics were too thin for the runtime 500s, so the logs script now persists per-fixture build/server/deployment logs into the uploaded artifact for the next run. |
| Official deploy suite `1/64` webpack baseline after custom 404 fallback | runtime-bug | 2026-06-19 run `27832219679` selected the same 17 tests at adapter `aee824b` with runtime main including `2db02fb`: still failed. The `critters` compile failure is still gone, but `404-page-app` remains red because the official fixture uses a Pages Router custom 404 with `_app`; the runtime static `/404` fallback fix is not enough for server-rendered Pages Router 404 semantics. The same broader classes remain: Next canary `cache-components`/`ppr-root-param-rsc-fallback` build failures with `TypeError: y.get is not a function`, App Router runtime 500/timeouts in `front-redirect-issue`, `metadata-navigation`, and `with-router`, missing image `srcset`, and Pages Router `router-is-ready-app-gip` assertions. This run showed that uploaded diagnostics dropped dotfile logs, so artifact staging was changed afterward to use non-hidden copies such as `adapter-build.log`, `adapter-server.log`, and `brrrd-harness/deployment.json`. |
| Official deploy suite `1/64` webpack baseline after OpenTelemetry stub fix | runtime-bug | 2026-06-19 run `27834612802` selected the same 17 tests at adapter `7c7f73c`: 12 passed and 5 failed. The prior `trace.getTracerProvider is not a function` runtime crash is gone, and the previously failing `front-redirect-issue`, `metadata-navigation`, and `with-router` suites now pass. Remaining failures are narrower: `404-page-app` renders the custom 404 body but returns HTTP 200 instead of 404, `cache-components` and `ppr-root-param-rsc-fallback` fail during Next canary static generation with `TypeError: y.get is not a function`, `next-image-new/image-from-node-modules` still misses the expected `srcset`, and `router-is-ready-app-gip` still fails Pages Router runtime assertions. |
| Official deploy suite `1/64` webpack after routing/runtime/artifact fixes | pass | 2026-06-19 run [`27843838172`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27843838172) at adapter `5146d19` selected the smoke shard and passed. This is the first green webpack smoke after the custom Pages Router 404 status, cache/PPR build, image static asset, router readiness, metadata navigation, front redirect, and runtime external packaging fixes landed. |
| Official deploy suite `1/64` Turbopack before non-SSR runtime-root patch | adapter-bug | 2026-06-19 run [`27843838202`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27843838202) at adapter `5146d19` selected the smoke shard and failed only `test/e2e/app-dir/front-redirect-issue/front-redirect-issue.test.ts`. Local repro showed Turbopack route handlers can use `.next/server/chunks/[turbopack]_runtime.js` in addition to `.next/server/chunks/ssr/[turbopack]_runtime.js`; the adapter patched only the SSR runtime wrapper, so route-handler chunk loading resolved against `/bundle/app.js` instead of packaged `/bundle/.next`. |
| Official deploy suite `1/64` Next default before non-SSR runtime-root patch | adapter-bug | 2026-06-19 run [`27843838310`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27843838310) at adapter `5146d19` had the same remaining `front-redirect-issue` failure as the forced Turbopack run. Next canary defaulted this shard to the same Turbopack-shaped route runtime behavior. |
| Official deploy suite `1/64` webpack current smoke | pass | 2026-06-19 run [`27844534055`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27844534055) at adapter `9276911` passed. |
| Official deploy suite `1/64` Turbopack current smoke | pass | 2026-06-19 run [`27844534077`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27844534077) at adapter `9276911` passed after the adapter patched both Turbopack server runtime wrappers: `server/chunks/ssr/[turbopack]_runtime.js` and `server/chunks/[turbopack]_runtime.js`. |
| Official deploy suite `1/64` Next default current smoke | pass | 2026-06-19 run [`27844534013`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27844534013) at adapter `9276911` passed. |
| Official deploy suite `1/64` webpack build/test split smoke | pass | 2026-06-19 run [`27845076389`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27845076389) at adapter `11c37ba` passed with separate `plan`, `build`, and `Tests (1/64, webpack)` jobs. |
| Official deploy suite `1/64` Turbopack build/test split smoke | pass | 2026-06-19 run [`27845076350`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27845076350) at adapter `11c37ba` passed with separate `plan`, `build`, and `Tests (1/64, turbopack)` jobs. |
| Official deploy suite `1/64` Next default build/test split smoke | pass | 2026-06-19 run [`27845076374`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27845076374) at adapter `11c37ba` passed with separate `plan`, `build`, and `Tests (1/64, next-default)` jobs. |
| Full official deploy suite `webpack` baseline | adapter-bug/runtime-bug/unsupported-yet | 2026-06-19 run [`27845591271`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27845591271) at adapter `518de0f` completed with 2/64 test shards passing and 62/64 failing. The run proved that `group=all` fan-out works, but exposed broad correctness gaps. First high-signal classes: Edge app/page/API outputs are intentionally rejected by the adapter (`unsupported-yet` unless brrrd adds a Fetch/Edge route execution bridge), middleware WASM refs were copied but not installed in the runtime bridge (`runtime-bug`, fixed afterward in brrrd `8323700`), several fixtures lack Adapter API `ctx.routing.dynamicRoutes.sourceRegex` for dynamic routes (`adapter-bug`, needs routing fallback/oracle design), and Pages prerender copy can collide on extensionless dynamic static paths (`adapter-bug`, `EISDIR` copying `[post].html` to `static/[post]`). |
| Official deploy suite `18/64` webpack after middleware WASM bridge fix | runtime-bug/unsupported-yet/adapter-bug | 2026-06-19 run [`27850508339`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27850508339) at adapter `6412051` with brrrd `8323700` still failed, but narrowed the WASM result: `edge-can-use-wasm-files` middleware assertions now pass, proving middleware `wasm[].name`/`filePath` reaches the runtime and installs `WebAssembly.Module` bindings before evaluating Next's middleware bundle. The remaining WASM failure is the Edge API route case (`pages/api/add`, runtime=edge), which is the broader Edge route output support gap. The same shard also failed `streaming-ssr` because the fixture emits multiple Edge Pages/API outputs, and exposed deploy diagnostics for `missing ctx.routing.dynamicRoutes sourceRegex` and `EISDIR` dynamic prerender copy collisions. |
| Official deploy suite `18/64` webpack after route regex/storage fixes | unsupported-yet/runtime-bug | 2026-06-19 run [`27851276207`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27851276207) at adapter `f473b1d` still failed, but the previous adapter build blockers are gone: the log contains no `sourceRegex`, `EISDIR`, or `Could not resolve "critters"` failures. Remaining high-signal failures are `app-simple-routes`, `edge-can-use-wasm-files`, and `streaming-ssr` hitting the intentional Edge app/page/API route rejection; `auto-export` serving generic 404 HTML where exported Pages Router routes should render; and `i18n-support-same-page-hash-change`, which failed in the same 404/export-routing class but needs a clean single-fixture rerun to separate from interleaved harness logs. |
| Official deploy suite `18/64` webpack after dynamic static/i18n aliases | unsupported-yet | 2026-06-19 run [`27852695785`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27852695785) at adapter `8edb980` still failed, but `auto-export` and `i18n-support-same-page-hash-change` both finished on retry 0. The log still contains no `sourceRegex`, `EISDIR`, or `critters` regressions. Remaining failures in this shard were `app-simple-routes`, `edge-can-use-wasm-files`, and `streaming-ssr`, all failing during deploy because the adapter intentionally rejected Edge app/page/API route outputs. At this point the blocker was to model `middleware-manifest.functions` and add an Edge route execution bridge instead of treating Edge routes as node handlers. |
| Local Edge app/page/API route bridge smoke | pass | 2026-06-19 local verification: adapter now models `middleware-manifest.functions` as `manifest.edgeFunctions`, route entries emit `runtime: "edge"` plus `edgeFunction`, and brrrd runtime loads those chunks through the Edge bridge instead of the Node handler bundle. `/tmp/brrrd-edge-fixture-3F939G` built with `next@16.3.0-preview.3` and `next build --webpack`; the manifest is `schemaVersion: 5`, contains `edgeFunctions.app-api-edge-route`, and local `brrrd <dist/brrrd>` returned 200 JSON for `/api/edge` plus 200 HTML for `/`. |
| Official deploy suite `18/64` webpack after Edge function bridge | adapter-bug/runtime-bug | 2026-06-19 run [`27854074833`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27854074833) at adapter `f5c6615` with brrrd `b28380e` still failed, but the previous Edge route deploy blocker is gone. `app-simple-routes` and `edge-can-use-wasm-files` finished on retry 0, including the Edge API WASM case that previously failed at deploy time. The only remaining failing fixture is `streaming-ssr`: `/api/user/login` matches the dynamic `[id]` API route, `/hello/` returns 404 after the trailing-slash redirect, and Edge Pages Router pages such as `/router` and `/multi-byte` return the generic 404. Next focus is Pages Router Edge route specificity plus trailing-slash/static page serving semantics. |
| Official deploy suite `18/64` webpack after static route/body bridge fixes | pass | 2026-06-19 run [`27855198076`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27855198076) at adapter `97d2315` passed with separate `plan`, `build`, and `Tests (18/64, webpack)` jobs. This confirms the prior local `streaming-ssr` smoke under the official harness: exact routes supplemented from Next `routes-manifest.staticRoutes`, Pages API empty GET body handling, and the generalized Next runtime missing `require()` policy did not regress the shard. |
| Official deploy suite `18/64` Turbopack before cache handler policy fix | adapter-bug | 2026-06-19/20 run [`27855520372`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27855520372) at adapter `97d2315` failed during fixture build before deploy. The first failure class was Next canary Turbopack Edge app-route template expansion with adapter-injected modern `cacheHandlers.default`; local reproduction showed the same `edge-app-route.js` parse failure when modern cache handlers were registered, while legacy `cacheHandler` alone got past that stage. Owner: adapter config policy. Retry after project-local cache handler materialization and bundler-aware modern handler registration. |
| Official deploy suite `18/64` Next default before cache handler policy fix | adapter-bug/next-assumption | 2026-06-19/20 run [`27855520382`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27855520382) at adapter `97d2315` failed in the same Turbopack-shaped build path because Next canary defaulted unqualified `next build` to Turbopack. That run also exposed one fixture-specific Next default constraint: `edge-can-use-wasm-files` experimental mode carries webpack config without a Turbopack config. Owner: adapter for cache handler policy; upstream/fixture classification for webpack-config-under-default-Turbopack if it remains after the cache handler fix. |
| Local Next canary cache handler policy smoke | pass | 2026-06-19/20 local verification with `next@16.3.0-canary.59`: `npm test` passed 56/56; a temp App Router fixture with an Edge route built successfully with `next build --turbopack` and unqualified `next build` after the adapter materialized cache handler files under `node_modules/.cache/@brrrd/adapter/` and skipped modern `cacheHandlers.default` for Turbopack. A webpack build without the canary Edge-route self-failure also passed with the same project-local support files. This proves the adapter no longer depends on package-manager symlinks or external absolute cache handler paths for these axes. |
| Official deploy suite `18/64` webpack after project-local cache handler policy | pass | 2026-06-19/20 run [`27856106957`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27856106957) at adapter `6bc9a19` passed. This confirms project-local cache handler materialization did not regress the previously green webpack shard. |
| Official deploy suite `18/64` Turbopack after project-local cache handler policy | pass | 2026-06-19/20 run [`27856106970`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27856106970) at adapter `6bc9a19` passed. This confirms the prior `edge-app-route.js` template parse failure and cache handler path escape panic are fixed for the forced Turbopack axis. |
| Official deploy suite `18/64` Next default after project-local cache handler policy | next-assumption | 2026-06-19/20 run [`27856106933`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27856106933) at adapter `6bc9a19` failed only `test/e2e/edge-can-use-wasm-files/index.test.ts` in the "middleware can use wasm files with the experimental modes on" subcase. The same run passed the Edge API WASM case and the regular middleware WASM cases, so the previous adapter cache handler build blocker is gone. The remaining failure happens before deploy because Next default uses Turbopack and the fixture carries a `webpack` config with no `turbopack` config; Next aborts with "This build is using Turbopack, with a `webpack` config and no `turbopack` config." Owner: Next fixture/default-bundler assumption; retry when the fixture adds an explicit bundler choice or Turbopack config. |
| Full official deploy suite `webpack` partial run after project-local cache handler policy | adapter-bug/runtime-bug/next-assumption | 2026-06-19/20 run [`27856463555`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27856463555) is still running, but the first failed shards already exposed two adapter/compiler blockers fixed locally afterward: Edge RSC outputs such as `app/app-ssr-edge/page.rsc` can carry Adapter API `outputs[].edgeRuntime` even when `middleware-manifest.functions` has no matching record, and worker fixtures can reference traced `.wasm` runtime chunks from Node handlers. Local changes now compile `edgeRuntime` as the primary source for `manifest.edgeFunctions` with `middleware-manifest.functions` fallback, preserve `entryKey`, and teach the app handler bundle to load `.wasm` as binary. Remaining observed failures from shards 2-5/8-9 include middleware request override/draft-mode behavior, i18n/default-locale rewrite semantics, static generation status codes, prerender encoding, fallback collision behavior, and cache component timing; these are not considered fixed by the Edge metadata/WASM packaging change. |
| Official deploy suite `8/64,9/64` webpack after Adapter API edgeRuntime/WASM loader fixes | runtime-bug/adapter-bug | 2026-06-19/20 run [`27857336233`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27857336233) at adapter `e9a5508` with brrrd `8db0d75` still failed, but the targeted build blockers are gone. In shard `8/64`, `with-exported-function-config` finished on retry 0, proving Edge RSC output metadata can now come from Adapter API `outputs[].edgeRuntime` without a matching `middleware-manifest.functions` record. In shard `9/64`, `worker.test.ts` no longer fails at esbuild `.wasm` loading; the WASM worker assertion passes, while only the `NEXT_DEPLOYMENT_ID` worker env assertion remains. Remaining failures are now behavior classes: `prerender-encoding`, `static-generation-status`, `static-rsc-cache-components`, unprefixed `i18n-ignore-rewrite-source-locale`, and `middleware-fetches-with-body` returning `Proxy Phase Failed` for body-preserving proxy cases. |
| Local official fixtures `prerender-encoding` and `static-generation-status` webpack after prerender metadata fixes | pass | 2026-06-20 local verification: `prerender-encoding/prerender-encoding.test.ts` passed 1/1 and `static-generation-status/index.test.ts` passed 5/5. Root causes: decoded App prerender public paths such as `/sticks & stones` were emitted without encoded public aliases, so encoded requests fell through to dynamic handlers; App prerender `.meta` files carried redirect/not-found status and headers, but the brrrd manifest/runtime route contract had no per-filesystem-route response metadata. Adapter now emits encoded aliases for static/prerender/App RSC data routes and attaches `.meta` status/headers to prerender routes. brrrd runtime now honors route-level status/headers for static/prerender outcomes. Regression coverage: adapter `extractAppPrerenderResponseMeta`, `App prerender response metadata is emitted on filesystem routes`, `prerendered decoded public paths also match encoded request paths`, and brrrd `prerender_route_metadata_controls_response_status_and_headers`. |
| Local official group `8/64` webpack after prerender metadata fixes | pass | 2026-06-20 local run `npm run harness:group -- --group 8/64 --bundler webpack --name shard-8-prerender-meta-encoding-fixed --concurrency 1` passed all 16 selected deploy fixtures on retry 0 under Next checkout `.node-version` Node 20. This confirms the local fixture fixes in the broader shard: `prerender-encoding`, `static-generation-status`, `metadata-static-file`, `global-not-found`, `draft-mode`, SCSS, script-loader, and exported function config all passed without retry. |
| Local official group `8/64` Turbopack after prerender metadata fixes | pass | 2026-06-20 local run `npm run harness:group -- --group 8/64 --bundler turbopack --name shard-8-turbopack-prerender-meta-encoding-smoke --concurrency 1` passed all 16 selected deploy fixtures on retry 0. This is the same shard as the webpack run, proving the `.meta` response metadata and encoded public alias route contract is not tied to webpack-only output shape. |
| Local official group `8/64` Next default after prerender metadata fixes | next-assumption | 2026-06-20 local run `npm run harness:group -- --group 8/64 --bundler next-default --name shard-8-next-default-prerender-meta-encoding-smoke --concurrency 1` selected the same 16 fixtures. The targeted metadata/alias fixtures passed (`prerender-encoding`, `static-generation-status`, metadata static file, global not-found, i18n/basepath rewrite, etc.), but the shard failed only `script-loader`. Preserved fixture run `script-loader-next-default-preserve` shows the build was `Next.js 16.3.0-canary.59 (Turbopack)` and the emitted `.next/server/pages/page1.html` / `dist/brrrd/static/page1` contain the expected Turbopack `/_next/static/chunks/` siblings after `scriptBeforeInteractive`. The failure comes from the fixture using `isTurbopack`, which in Next test utils is keyed only by `IS_TURBOPACK_TEST`; the `next-default` axis intentionally clears both explicit bundler flags, so the test takes the webpack-only `/_next/static/chunks/main` selector against Turbopack HTML. Forced `turbopack` passes the same shard. Owner: Next fixture/default-bundler assumption, not adapter/runtime. Retry when the official tests expose a default-bundler-aware assertion path, or run the explicit `turbopack` axis for Turbopack conformance. Regression guard: `test/local-harness.test.mjs` verifies `next-default` keeps `IS_WEBPACK_TEST` and `IS_TURBOPACK_TEST` unset. |
| Official deploy suite `9/64` webpack after deployment-id harness fix | runtime-bug | 2026-06-19/20 run [`27858412172`](https://github.com/burrr-ai/brrrd-adapter/actions/runs/27858412172) at adapter `2a0729f` failed, but the targeted worker deployment-id failure is gone. The harness now exports `NEXT_DEPLOYMENT_ID` before `next build`, and `e2e-logs.sh` reads the last marker line, so `worker.test.ts` finished on retry 0. The shard also confirms the metadata static file fixture still passes, so the previous `apple-icon`/metadata route work did not regress. Remaining hard failures are `static-rsc-cache-components` returning browser-visible 500s and `elapsed = NaN`, plus unprefixed `i18n-ignore-rewrite-source-locale` requests returning default-locale 404 HTML while `/en`, `/sv`, and `/nl` pass. `middleware-fetches-with-body` still shows flaky `Proxy Phase Failed` responses for body-preserving proxy cases before eventually finishing on retry 2, so it remains a runtime body/proxy fidelity follow-up even though it was not the final blocking suite in this run. |
| Local official `i18n` locale-disabled routing fixtures | pass | 2026-06-20 local verification: `i18n-ignore-rewrite-source-locale/rewrites.test.ts` passed 8/8 and `i18n-ignore-redirect-source-locale/redirects.test.ts` passed 16/16 on the local `brrrd <dist/brrrd>` deploy harness with webpack. The adapter now preserves `routes-manifest` rule-level `locale: false` and emits `routing.i18n`; brrrd runtime compiles that to shared default-locale candidate semantics for redirects and rewrites instead of special-casing individual paths. Regression coverage: adapter `compileRouting`/`extractRewriteSupplement` tests and brrrd `routing::plan::tests::locale_false_*`. |
| Local official group `9/64` webpack after i18n locale-disabled routing fix | runtime-bug/flaky-infra | 2026-06-20 local run `npm run harness:group -- --group 9/64 --bundler webpack --name shard-9-after-i18n-locale-false --concurrency 1` still exited red, but `test/e2e/i18n-ignore-rewrite-source-locale/rewrites.test.ts` finished on retry 0. Remaining failures are separate classes: `static-rsc-cache-components` still returns a browser-visible 500 and `elapsed = NaN` (`runtime-bug`), while `next-config-ts-native-ts/export-default` fails before adapter deploy under the local Node 24 harness with `ERR_REQUIRE_ASYNC_MODULE` while loading `next.config.ts` (`flaky-infra`/harness environment; retry under the official Node version before classifying as adapter/runtime). Other shard fixtures including `worker`, `middleware-fetches-with-body`, and `next-image-new/unoptimized` finished on retry 0 in this run. |
| Local official fixture `static-rsc-cache-components` webpack after App RSC data static serving | pass | 2026-06-20 local run `npm run harness:fixture -- --fixture test/e2e/app-dir/static-rsc-cache-components/static-rsc-cache-components.test.ts --bundler webpack --name static-rsc-soft-nav-fixed` passed. Root cause: App Router soft navigation requests `/alpha` with `RSC: 1`; brrrd was skipping prerender HTML and falling through to the dynamic `/[slug]` handler, causing `NEXT_STATIC_GEN_BAILOUT` or hard navigation. Adapter now packages App prerender `.rsc` / `.segment.rsc` files into the static store and emits exact static routes; runtime resolves full-route RSC requests to those data artifacts before dynamic routes and serves `.rsc` with Next data/deployment headers. Regression coverage: adapter `onBuildComplete exposes PPR segment prefetch artifacts through the static store`, adapter routing tests for App prerender data routes, brrrd `routing::plan::tests::rsc_request_uses_prerendered_flight_data_before_dynamic_route`, and brrrd `handler::response::tests::rsc_static_responses_carry_next_data_headers`. |
| Local official group `9/64` webpack after App RSC data static serving | flaky-infra/runtime-bug | 2026-06-20 local run `npm run harness:group -- --group 9/64 --bundler webpack --name shard-9-after-static-rsc-soft-nav --concurrency 1` still exited red, but `static-rsc-cache-components`, i18n locale-disabled routing, metadata static file, worker, dynamic optional fallback, and image unoptimized fixtures finished on retry 0. The hard red is still `next-config-ts-native-ts/export-default`, which fails before adapter deploy under the local Node 24 harness with `ERR_REQUIRE_ASYNC_MODULE`; retry under the official Node version before classifying as adapter/runtime. `middleware-fetches-with-body` returned transient `Proxy Phase Failed` responses on retry 0 but passed on retry 1, so body-preserving proxy fidelity remains a runtime follow-up rather than a current hard blocker for this shard. |
| Local official group `9/64` webpack under Next `.node-version` | pass | 2026-06-20 local run `npx -y node@20 scripts/local-harness.mjs group --group 9/64 --bundler webpack --name shard-9-node20-official-env --concurrency 1` passed all 16 selected deploy fixtures on retry 0. The previous `next-config-ts-native-ts/export-default` red was a local Node 24-only harness-environment mismatch: under Node 20 the official fixture skips because `process.features.typescript` is absent. `scripts/local-harness.mjs` now follows `/Users/ggm/work/next.js/.node-version` by default and has regression tests for the node runner selection, preventing this false red from returning. A post-fix deploy fixture smoke also passed with the default `npm run harness:fixture -- --fixture test/e2e/app-dir/static-rsc-cache-components/static-rsc-cache-components.test.ts --bundler webpack --name static-rsc-default-node-selector-fixed`. |
| Local official group `9/64` Turbopack under Next `.node-version` | pass | 2026-06-20 local run `npm run harness:group -- --group 9/64 --bundler turbopack --name shard-9-turbopack-current --concurrency 1` passed all 16 selected deploy fixtures on retry 0. This extends the shard-9 App RSC static data, worker WASM, metadata static file, body-preserving middleware fetch, i18n locale-disabled rewrite, and dynamic optional root fallback coverage to forced Turbopack output shape. |
| Local official group `9/64` Next default under Next `.node-version` | pass | 2026-06-20 local run `npm run harness:group -- --group 9/64 --bundler next-default --name shard-9-next-default-current --concurrency 1` passed all 16 selected deploy fixtures on retry 0. Next canary used its default build path with explicit bundler test flags unset, so shard 9 now has evidence across webpack, forced Turbopack, and current canary default behavior. |
| Local official metadata route/runtime fixtures from `49/64-64/64` webpack | pass | 2026-06-21 follow-up reduced the metadata bucket to three representative fixtures and fixed/verified them locally without claiming the whole 49-64 batch is green. `metadata-dynamic-routes` passes at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T06-46-10-728Z-verify-metadata-dynamic-after-font`, `metadata-edge` passes at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T06-46-10-732Z-verify-metadata-edge-after-font`, and `metadata-font` passes at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T06-45-08-714Z-verify-metadata-font-assets`. Root causes: static metadata route handlers need prerender response headers/content type preserved from Next metadata, Edge metadata functions need support assets separated from executable JS/WASM artifacts, and Edge inline asset fetch must follow Next `fetchInlineAsset` semantics by using `String(input)` for `blob:` URLs. Regression coverage: adapter `onBuildComplete preserves static route handler prerender response metadata`, adapter `onBuildComplete emits edge app route function metadata and artifacts`, adapter `onBuildComplete copies App Route project-relative traced assets into runtime fs`, brrrd JS bridge `edge asset fetch follows Next inline asset String(input) semantics`, full `npm --prefix /Users/ggm/work/brrrd-adapter test`, `node --test crates/brrrd/src/js/node/middleware_bridge.test.mjs`, and `just build`. Classification: semantic fix, no temporary shim. |
| Local official legacy image optimizer fixtures from `49/64-64/64` webpack | pass | 2026-06-21 follow-up reduced the legacy `next/image` optimizer bucket to two representative fixtures and fixed/verified them locally without claiming the whole 49-64 batch is green. `next-image-legacy/default` passes at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T07-09-20-557Z-verify-next-image-legacy-default-final` with 28/28 assertions, and `next-image-legacy/unicode` passes at `/Users/ggm/work/.brrrd-local-harness/2026-06-21T07-10-20-714Z-verify-next-image-legacy-unicode-final` with 5/5 assertions. Root causes: the adapter manifest did not carry Next image policy into brrrd, and the runtime image optimizer only returned local static bytes without width resizing, remote URL allow-list validation/fetching, or local traversal rejection before URL normalization. Adapter now emits `images` config from Next build config into the brrrd manifest; runtime serves `/_next/image` from that manifest policy, validates local/remote patterns, fetches allowed remote images, preserves non-decodable formats as raw bytes, and resizes JPEG/PNG/WebP without upscaling. A related routing conformance drift was fixed while running full brrrd tests: non-RSC `NodeRoute.resolvedPathname` now uses the matched route id/pathname, while RSC requests keep the invocation page path. Regression coverage: adapter `writeManifest records Next image optimizer policy`, brrrd `next_image_optimizer_*` response tests, brrrd `@next/routing` oracle test, full `npm --prefix /Users/ggm/work/brrrd-adapter test`, full `cargo test -p brrrd --lib`, `cargo build -p brrrd`, and the two representative official fixtures. Classification: semantic fix, no temporary shim. |
| Local official Segment Cache / PPR representative fixtures | pass | 2026-06-21/22 local harvest closed the open `segment-cache/PPR prefetch` ledger bucket after structural runtime/cache fixes. `partial-prefetching-config` passed at `/Users/ggm/work/.brrrd-local-harness/2026-06-22T03-20-22-993Z-ppr-partial-prefetch-after-shell-tail-combine`; `vary-params` passed at `/Users/ggm/work/.brrrd-local-harness/2026-06-22T04-11-06-976Z-ppr-runtime-prefetch-resume`; `prefetch-inlining` passed at `/Users/ggm/work/.brrrd-local-harness/2026-06-22T04-21-56-801Z-ppr-revalidate-tag-expiration`; `vary-params-base-dynamic` passed 15/15 at `/Users/ggm/work/.brrrd-local-harness/2026-06-22T13-32-22-322Z-ppr-vary-full-after-cache-tee` in 557s and closed the persistent ledger entry. Root causes: brrrd treated all `next-router-prefetch` requests as static prefetches, while Next only treats value `1` as `isPrefetchRSCRequest`; runtime prefetch value `2` must be eligible for PPR resume; App Shell RSC prefetch value `3` needs fallback route params but must not become a `next-resume` POST; static App prerenders need `prerender-manifest.routes[*].experimentalBypassFor` so server-action POST bypasses static bytes; and the modern cache handler must tee CacheHandler entry streams on `set()` like Next's default handler instead of consuming the caller's single-use stream. On-demand revalidation also required cache tag tombstone timestamps plus static/prerender route tag checks so revalidated build-time prerenders fall through to the dynamic handler instead of serving stale static bytes. Regression coverage: adapter `modern cache handler delegates tag expiration to brrrd ops`, `modern cache handler preserves the caller entry stream while storing`, `extractAppPrerenderResponseMeta preserves static prerender bypass conditions`, `static App prerender routes keep server action bypass metadata`; brrrd cache backend/tag-expiration tests; brrrd PPR resume/header-value/fallback route param tests; `cargo test -p brrrd cache --lib`; `cargo test -p brrrd ppr_ --lib`; `cargo build -p brrrd`; full `npm --prefix /Users/ggm/work/brrrd-adapter test`; focused partial official patterns; and the full `vary-params-base-dynamic` fixture. Operational note: `vary-params-base-dynamic` is still a slow fixture and should be harvested with a dedicated slow bucket or timeout greater than 10 minutes, but it is no longer an open correctness failure. Classification: semantic fix, no temporary shim. |
| Local official fixture `non-ascii-cache-tags` webpack after PPR reentry pathname fix | pass | 2026-06-22 ledger-aware run `harness:harvest -- --fixtures test/e2e/app-dir/non-ascii-cache-tags/non-ascii-cache-tags.test.ts --bundlers webpack --parallel 1 --name non-ascii-cache-tags-resolved-pathname-ledger --timeout-ms 900000 --cleanup-target-artifacts passed --brrrd-bin /Users/ggm/work/brrrd/target/debug/brrrd` passed 6/6 in 37s and closed the persistent ledger entry. Artifact root: `/Users/ggm/work/.brrrd-local-harness/2026-06-22T07-29-28-137Z-non-ascii-cache-tags-resolved-pathname-ledger`. Root cause: stale static/prerender reentry and PPR resume were passing brrrd internal handler ids such as `_slug_` as Next `resolvedPathname`; cacheComponents/PPR revalidation needs the concrete public prerender key, including percent-decoded non-ASCII paths like `/🎉`, so Next can match `prerender-manifest.routes` and the App page cache entry. brrrd now derives reentry `resolvedPathname` from the invocation path, decodes UTF-8 path segments, and preserves encoded slash/backslash segments. Regression coverage: brrrd `reentry_resolved_pathname_uses_decoded_public_path`, `reentry_resolved_pathname_keeps_encoded_slash_segments_encoded`, `cargo check -p brrrd --lib`, `cargo build -p brrrd`, and the official fixture. Classification: semantic runtime bridge contract fix, not a fixture branch. |
| Local official fixtures `invalid-static-asset-404-{app,pages}-base-path` webpack after basePath fallback contract | pass | 2026-06-22 ledger-aware run `harness:harvest -- --fixtures test/e2e/invalid-static-asset-404-app/invalid-static-asset-404-app-base-path.test.ts,test/e2e/invalid-static-asset-404-pages/invalid-static-asset-404-pages-base-path.test.ts --bundlers webpack --parallel 2 --name basepath-custom-404-fix --timeout-ms 900000 --defer-after-ms 300000 --max-deferred-running 5 --cleanup-target-artifacts passed --brrrd-bin /Users/ggm/work/brrrd/target/debug/brrrd` passed both fixtures in about 35s and closed the two persistent ledger entries. Artifact root: `/Users/ggm/work/.brrrd-local-harness/2026-06-22T07-39-57-534Z-basepath-custom-404-fix`. Root cause: adapter emitted basePath into public route patterns, but not as a first-class `routing.basePath` manifest capability when i18n was absent; brrrd custom-not-found fallback therefore looked for `/404` / `/_not-found` instead of `/base/404` / `/base/_not-found`. Adapter now emits `routing.basePath` independently from i18n, and brrrd routing plan uses it for basePath-prefixed custom 404 candidates while preserving Next's plain 404 behavior for invalid `/_next/static` asset paths. Regression coverage: adapter `routing manifest carries basePath independently from i18n`, brrrd `base_path_no_match_uses_prefixed_static_404_fallback`, brrrd `base_path_next_static_miss_bypasses_custom_404_fallback`, `npm --prefix /Users/ggm/work/brrrd-adapter test -- test/routing.test.mjs test/fail-fast.test.mjs`, `cargo test -p brrrd --lib base_path_`, `cargo check -p brrrd --lib`, `cargo build -p brrrd`, and the two official fixtures. Classification: semantic adapter/runtime routing contract fix, not a fixture branch. |
| Local official open-ledger refresh after basePath custom 404 | adapter-bug/runtime-bug/slow-deferred | 2026-06-22 direct fixture harvest `harness:harvest -- --fixtures <13 open ledger fixtures> --bundlers webpack --parallel 4 --name open-ledger-refresh-after-basepath-404 --timeout-ms 900000 --defer-after-ms 300000 --max-deferred-running 5 --cleanup-target-artifacts passed --brrrd-bin /Users/ggm/work/brrrd/target/debug/brrrd` wrote `/Users/ggm/work/.brrrd-local-harness/2026-06-22T07-41-51-618Z-open-ledger-refresh-after-basepath-404/harvest-summary.json`. Result: 2 pass / 11 fail, closing `use-router-bfcache-id` and `fallback-shells` without additional production code changes. At that time remaining buckets were `bundler output shape` (`prerender-native-module` dependency install/trace), `metadata/static asset` (`use-cache-metadata-route-handler`), Pages rewrite/data URL semantics split across `getinitialprops`, `getserversideprops`, and `link-with-api-rewrite`, `i18n/basePath`, `middleware/proxy` build blocker, unclassified build/test failures, and slow-deferred `segment-cache/vary-params-base-dynamic`; later rows record all but `prerender-native-module` closed. |
| Local official Pages visible URL / RouterContext / custom 500 fixtures after dispatcher batch | pass | 2026-06-22 follow-up closed the Pages rewrite/data cluster from the open ledger. First, brrrd routing now separates handler-visible invocation URL from destination lookup URL: rewrites use the source URL for `req.url` / `asPath`, while route lookup and `__NEXT_INIT_URL` target the rewritten destination. Representative fixtures `getinitialprops` and `link-with-api-rewrite` passed together at `/Users/ggm/work/.brrrd-local-harness/2026-06-22T08-00-06-147Z-pages-rewrite-visible-url-fix`. Second, adapter bundling aliases direct `next/dist/shared/lib/router-context.shared-runtime` imports to the Pages Router vendored singleton and normalizes the vendored export shape, so `RouterContext.Provider` and `next/router` share the same context. Third, the dispatcher now catches ordinary Pages handler failures and renders the custom `500` route, running `_error` first with `customErrorRender` metadata when present, matching Next's error-reporting side-effect path. The full representative `getserversideprops` fixture passed at `/Users/ggm/work/.brrrd-local-harness/2026-06-22T08-23-55-939Z-pages-gssp-custom-500-recovery`. Regression coverage: brrrd rewrite oracle tests covering visible-vs-lookup URLs, `cargo check -p brrrd --lib`, `cargo build -p brrrd`, adapter `bundleAppHandler aliases Pages RouterContext direct imports to Next vendored singleton`, adapter `dispatcher renders custom Pages 500 route when a Pages handler throws`, `node --test test/dispatcher.test.mjs`, `node --test test/fail-fast.test.mjs`, and the three official fixtures. Classification: semantic routing/compatibility/dispatcher fix, not a fixture branch. |
| Local official fixture `prerender.test.ts` webpack after Pages ISR/fallback cache policy batch | pass | 2026-06-22 ledger-aware fixture run `BRRRD_HARNESS_COPY_PACKAGE=1 npm --prefix /Users/ggm/work/brrrd-adapter run harness:fixture -- --fixture test/e2e/prerender.test.ts --bundler webpack --name pages-prerender-deploy-cache-policy --timeout-ms 1800000 --brrrd-bin /Users/ggm/work/brrrd/target/debug/brrrd` passed 63/63 and closed the persistent `slow fixture` ledger entry. Artifact root: `/Users/ggm/work/.brrrd-local-harness/2026-06-22T10-19-13-548Z-pages-prerender-deploy-cache-policy`. Root causes: adapter was publishing dynamic Pages prerender template data paths as exact static artifacts, brrrd served fallback:true shell bytes even after a concrete path had been generated, ISR TTL reentry could bypass the original prerender before it had been served once, and deploy-mode node revalidation responses needed the same `public, max-age=0, must-revalidate` cache policy as prerender static/data responses. Regression coverage: adapter dynamic-prerender template artifact/routing tests, brrrd `prerender_ttl_*`, `pages_fallback_shell_reenters_after_generated_path_is_cached`, `deploy_prerender_node_responses_use_prerender_cache_policy`, `html_prerender_static_responses_carry_cache_header`, `cargo build -p brrrd`, adapter `npm run build`, and the official fixture. Classification: semantic adapter/runtime prerender contract fix, not a fixture branch. |
| Local official fixture `prerender-native-module.test.ts` webpack after native build-policy cleanup | unsupported-yet | 2026-06-22 ledger-aware harvest `harness:harvest -- --fixtures test/e2e/prerender-native-module.test.ts --bundlers webpack --parallel 1 --name prerender-native-runtime-native-classification --timeout-ms 1800000 --cleanup-target-artifacts passed --brrrd-bin /Users/ggm/work/brrrd/target/debug/brrrd` reached the real adapter compatibility gate and recorded the open ledger entry as `Node/runtime API`. Earlier harness blockers were removed: pnpm `onlyBuiltDependencies` no longer conflicts with workspace `dangerouslyAllowAllBuilds`, and a local `python` wrapper lets sqlite3 build when only `python3` exists. The remaining failure is intentional architectural scope: the fixture's Pages Router `fallback: true` route can execute `getStaticProps` at request time for `/blog/second`, and that path loads sqlite3's `.node` addon. brrrd isolates do not support native Node addons, so passing this fixture requires a deliberate runtime strategy for native addons or a separate Node fallback execution model, not a compatibility shim. Harness regression coverage: `pnpm build policy keeps native allow-list and removes never-build conflicts`, `harvest reads deploy diagnostics for failures without Jest fixture inventory`, and the ledger state now records `Node/runtime API` instead of stale `bundler output shape`. |
| Full official deploy suite `turbopack` | pending | Run only after the next webpack blocker class is reduced or explicitly classified, so Turbopack output-shape bugs are not mixed with known generic adapter/runtime gaps. |
| Full official deploy suite `next-default` | pending | Run after webpack and forced Turbopack are classified; this axis records the current Next canary default build behavior. |

## Policy

Exclusions are temporary. Every deferred or excluded test must include a reason, owner, and condition for retry. A green
`1/64` smoke shard is a confidence signal only; declaring a bundler axis compatible requires all configured shards for
that axis to pass, or every remaining failure to be explicitly classified with owner and retry condition.
